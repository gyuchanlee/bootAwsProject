language: java
jdk:
  - openjdk11
branches:
  only:
    - master

# travis CI server home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

# when CI complete, alarm send email
notifications:
  email:
    recipients:
      - hoo788@gmail.com